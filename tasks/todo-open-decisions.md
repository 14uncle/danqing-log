# TODO: 待裁清单 (UI 视觉重构的遗留)

> 立的时机: 2026-09-13, UI 改造五模块全部闭环之后。
> **这份清单是这四条的唯一正文** —— SPEC §4 与 CLAUDE.md 只放一行指针,
> 别处不再抄数字 (本仓「同一内容写两处然后漂了」已复发 9 次)。
> 状态: **四条全部待用户裁决, 一条都没动。**

四条都是**查出来的**, 不是设计出来的 —— 在做别的模块时顺带照见, 当场判断
「不属于本轮、且动了会越权」, 故原样挂着。

---

## D1 · 浅色三支「面」token 几乎不存在

| token | 浅色渲染 | 对底 `#F0F8F6` 对比度 | 暗色对照 |
|---|---|---|---|
| `surface` | (251,253,253) | **1.06** | 1.37 |
| `surface_input` | (254,255,255) | **1.08** | 1.52 |
| `surface_variant` | (238,246,242) | **1.02** | 1.16 |

`surface_variant` 与底**只差 2/255** (Δ`L*` −0.74) —— 等于不存在。

**影响面 (查过代码, 不是猜的)**:

- 内容区行 hover / 斑马 —— **已在模块 5 修掉**, 产品侧自备
  `row_band_bg()` / `row_hover_bg()`, 与 token 无关了。
- **`Dropdown` 弹层 hover 行 —— 仍未修, 且产品够不着。**
  框架 `dropdown.rs:86` 取 `theme.surface_variant()` 作 hover 行底色,
  而弹层底是 `surface_input` (浅色下 ≈ 纯白)。按线性合成推算:
  hover `(238,246,242)` 压纯白只有 **~1.10:1** —— 鼠标划过下拉选项**看不出在哪行**。
  产品改不了 (组件在框架里), 只能改 token 或改组件自己的取值。
- 表头 / 设置卡 hover —— 同源, 未逐项核 (需真机复核)。

**没动的理由**: 公开 token, blast radius 跨产品 (pomodoro 等同吃);
且它**不是**模块 1 混合空间迁移造成的 (近白对那次变更是钝感的), 属既有设计问题。

**要裁的**: ① 改 `surface_variant` 的浅色取值 (跨产品, 要两仓联动)
/ ② 只改 `Dropdown` 组件内的 hover 取值 (局部, 但等于承认 token 不可用)
/ ③ 不改, 接受下拉 hover 不可见。

---

## D2 · 浅色语义色也 sub-AA

对浅色底 `#F0F8F6` 实测 (WCAG AA 正文线 = 4.5):

| | 对底 | 判定 |
|---|---|---|
| ERROR (191,46,46) | 5.34 | ✅ |
| INFO (56,117,189) | 4.38 | 差一点 |
| OK (41,143,82) | **3.78** | ❌ |
| WARN (179,128,20) | **3.18** | ❌ |
| DEBUG (140,140,148) | **2.97** | ❌ |

**没动的理由**: 用户对浅色的指示是「现值一行不动」, 且这套是用户验收过的
既有观感。暗色那套是新写的, 所以暗色我卡了 AA 下限, **浅色没卡** ——
拿护栏去卡浅色等于借护栏之名改浅色主题 (与框架 `theme.rs` 里
`no_theme_renders_a_translucent_surface_as_a_slab` 只卡上限同一条纪律)。

**要裁的**: ① 提亮浅色 WARN/OK/DEBUG (会改浅色观感, 要真机过目)
/ ② 接受 (日志正文本来就密, 语义色当扫描线用, 不追求长文可读性)
/ ③ 只提单 ≠ 全提 (代价是色板不再同亮度档)。

---

## D3 · 浅色两条着色路径的值本来就分叉

「整行着色」(`level_color`) 与「单元格/状态着色」(`level_cell_color` / `status_color`)
**原先各写了一套字面量**, 且它们不一样:

| | 整行着色 | 单元格 | Δ`L*` | Δ`E76` |
|---|---|---|---|---|
| ERROR | (191,46,46) | (194,54,54) | 1.66 | **3.43** |
| WARN | (179,128,20) | (184,128,5) | 0.55 | **4.15** |
| DEBUG | (140,140,148) | (143,143,153) | 1.21 | 1.65 |

JND ≈ 2.3 —— **前两支可感知** (虽然两种模式不会同屏出现, 所以用户没发现)。

**没动的理由**: 合并任一支都会改浅色的既有观感, 与「现值一行不动」冲突。
本轮的做法是**原样保留两条口径**, 只在 `view.rs` 的 `LevelPalette` 里
把它们并成两个构造函数 (`light_line()` / `light_cell()`) 并写明来源;
暗色是全新的没有包袱, **两条路径共用一套**。

**要裁的**: 合并到哪一支 (或都不合并, 永久保留两套)。

---

## D4 · 框架 `composite_over` 混在 sRGB 空间, 不是线性

`danqing/src/theme.rs:84` 的公开函数 `composite_over` 直接对**存储分量**
(sRGB 编码) 做 over 运算。但模块 1 之后 **GPU 在 linear 空间混合**。
两者不是一个东西:

- 它自己的测试 `composite_over_half_white_on_black_is_mid_gray` 把这个旧行为
  **钉死了** (线性混合下 50% 白压黑应为 ≈187, 不是 128)。
- 而 `contrast_ratio` 的文档 (`theme.rs:75`) 恰恰写着
  「半透明色请先经 `composite_over` 合成到底色再比较」—— **照着做就是量错对象**,
  正是「护栏全绿、屏幕全灰」那类事故的入口。
- `SceneTheme::surface()` (`theme.rs:638`) 也用它做构造期推导。

**现状**: 本仓新写的守卫一律绕开它, 自备线性合成
(`view.rs` 测试里的 `composited_luminance`), 并在注释里标明「别用它」。

**要裁的**: ① 改 `composite_over` 为线性 (要同步改它自己的测试 + 复核
`SceneTheme::surface()` 的输出变化) / ② 只修文档, 把陷阱写明 / ③ 加一个
线性版本另起名字, 旧的标 deprecated。

---

## 裁决记录

> 逐条落, 裁完把结论写进这里并从上面移到「已裁」。
