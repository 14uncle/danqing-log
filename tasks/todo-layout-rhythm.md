# TODO: layout-rhythm (模块 5 —— 展开块底色)

> spec: `docs/specs/SPEC-ui-redesign.md` §1 模块 5 / §3 | **无独立 plan**
> 状态: **待设计提案门**（改代码出真机截图，用户过目后才算过）
> 基线: 本仓 **109 绿** (51 + 50 + 8), 框架全绿 (2026-09-13 实测)

## 范围（2026-09-13 用户当面收窄）

原「三件事」**收窄成一件事** —— 用户澄清:

- 「**内容区块**」= **可展开行展开后的那块区域**, 不是整个表格区
  （原第 ③ 条「整个内容区给底色」与第 ② 条**是同一件事**）。
- **斑马行保留**（原第 ① 条「去掉斑马行」**撤销**）。
  附带: `surface_variant` 的 α 已由模块 2 重校, 斑马不再是一块灰板。

> **无独立 plan 是故意的**: 收窄后只剩一个任务, plan/todo 两道文档的仪式成本
> 大于收益。记录落在这里 + SPEC §3。

## 任务

- [x] **T1: 展开块给底色（略深）** —— 机器部分 ✅ 2026-09-13
  - 说明: 展开的子行原先与真实行**长得一模一样**（只差没有行号）, 用户分不清
    「这坨是第 1 行展开的」还是「又是几行日志」。
  - Acceptance: ① 新增 `expand_block_bg(theme)` —— **产品语义, 放产品侧**,
    与 `bookmark_color` 同一处理, **不扩公开 `Theme` trait**;
    ② 子行先铺该底色再画字; ③ **斑马不盖展开块**（块要靠单一底色读作「一整块」,
    交替条纹会切碎它）; ④ 选中/hover 仍压在块之上（那两态必须保持可见）
  - 实测（两个主题的知觉台阶**几乎相等** —— 模块 2 想要的跨主题一致性, 这里是自然达成的）:

    | | 底 | 块 | Δ`L*` |
    |---|---|---|---|
    | 浅色 | `#F0F8F6` (L\* 96.95) | `#E4EEEA` (L\* 93.27) | **−3.68** |
    | 暗色 | `#191920` (L\* 9.04) | `#111117` (L\* 5.26) | **−3.79** |

  - 回归锁 `expand_block_bg_is_a_slightly_darker_step_in_both_themes` **先红后绿**
    (先报 E0425 函数不存在), 断言两主题都「与底不同」且「**比底深**」——
    用户裁定的是「略深」, 不是略浅。
  - Files: `src/view.rs`
  - Scope: S

### Checkpoint: 设计提案门（用户真机过目）

- [ ] **暗色 + 浅色各一张截图, 确认展开块「读作一整块」且「略深」的分量合适**
- [ ] 暗色余地很窄（底 `L*` 仅 9.04, 再深很快就到黑）—— 若偏深/偏浅当场调数值,
      这一项本来就是一个可整体回退的小批次

## 偏离记录

1. **不做独立 plan**（见上, 范围收窄到一个任务）。
2. **SPEC §3 原「硬约束」段作废**: 它写「本模块必须给框架 `Theme` 加 token」,
   理由是「需要**两个**互不相同的不透明底色」。范围收窄后只要一个色, 且属产品语义
   —— 不动公开 API、不重钉 rev。已在 SPEC §3 原地改写并注明原因。
