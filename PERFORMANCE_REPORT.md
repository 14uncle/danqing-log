# 丹青日志 LogLens 性能报告

**测试时间**：2026-09-11
**测试环境**：Windows 11, Release 模式
**测试文件**：1GB (1,073,741,824 bytes)

## 性能总结

### 1GB 明文日志

| 指标 | 实测值 | 目标值 | 状态 |
|------|--------|--------|------|
| **索引** | 92ms | <425ms | ✅ |
| **搜索 (ERROR)** | 115ms | <69ms | ⚠️ |
| **搜索 (ERROR\|FATAL)** | 120ms | - | - |
| **随机访问** | 0.52µs/行 | - | - |

> 2026-09-15 复测: 搜索自 2026-09-15 起**默认大小写不敏感**（`(?i)` 前缀，
> 用户可写 `(?-i)` 恢复敏感），故比上表历史值（71ms / 111ms）慢约 1.6 倍。
> 目标值一列仍是 POC 期基线（<425ms / <69ms），其中 `<69ms` 是**敏感匹配时代**
> 的目标 —— 不敏感是产品决定，代价已实测并接受（spec case-insensitive §4）。

### 1GB JSONL 日志

| 指标 | 实测值 | 目标值 | 状态 |
|------|--------|--------|------|
| **索引** | 94ms | <425ms | ✅ |
| **字段过滤** | 40ms | <235ms | ✅ |
| **字段分析 (duration_ms, 全文件)** | 1001ms | ≤1500ms | ✅ |
| **字段分析 (跟随过滤, 36,123 行)** | 124ms | - | - |
| **搜索 (ERROR)** | 129ms | <69ms | ⚠️ |
| **搜索 (ERROR\|FATAL)** | 131ms | - | - |
| **随机访问** | 0.58µs/行 | - | - |

> 2026-09-15 复测 (同明文那处说明)。字段过滤一栏本次以 `level=error` (不敏感默认)
> 实测 —— 与敏感时代同量级: 键的 memmem 粗筛不动, 只换已提取 token 的比较算子。
>
> **字段分析 (v1.x, 2026-09-19 实测)**: `logbench demo-1gb.jsonl --analyze duration_ms`
> —— 402 万行单列聚合 1001ms (引擎 = `danqing-logfile` 顶层字段扫描器, 单遍零
> Value 树; 分位数 reservoir 上限 100 万值, 本文件超限故标「采样估计」;
> `status` 列同量级 1017ms)。跟随过滤时只扫命中行集 (`--filter level=ERROR` →
> 36,123 行 124ms, 分位数精确)。竞品锚: LogViewPlus 全量解析 1GB 在本机卡死
> (见竞品对比节), 这是付费层腿二的立身数字。

## 竞品对比

### 性能对比

| 场景 | 丹青日志 | klogg | LogViewPlus |
|------|----------|-------|-------------|
| **冷启动 (1GB)** | ~600ms | 秒级 | 慢 |
| **热启动 (1GB)** | ~90ms | 快 | 慢 |
| **搜索** | 115ms | 快 | 慢 |
| **JSONL 字段过滤** | 40ms | N/A | N/A |

> 这两行 2026-09-15 与上面两张实测表对齐（此前是本报告自己的旧值 69ms / 74ms ——
> 同一份文件里两个数字打架，是最容易让人抄错的那一类）。搜索一栏 = 默认大小写
> 不敏感的值，与 klogg 对比时它偏保守（klogg 默认敏感），别再按旧的 69ms 理解。

### 功能对比

| 功能 | 丹青日志 | klogg | LogViewPlus |
|------|----------|-------|-------------|
| mmap 秒开 | ✅ | ✅ | ❌ |
| JSONL 列化 | ✅ | ❌ | ❌ |
| 字段过滤 | ✅ | ❌ | ❌ |
| 嵌套展开 | ✅ | ❌ | ❌ |
| ANSI 颜色 | ❌ | ❌ | ✅ |
| 活跃维护 | ✅ | ❌ | ✅ |
| 价格 | $0 / $29 | 免费 | $45/$95 |

## 核心优势

### 1. 对 klogg：功能差异化

**klogg 痛点**：
- 停更 4 年（2022-06 最后稳定版）
- 273 open issue 未解决
- 无 JSONL 支持
- 无 ANSI 颜色支持

**我们的解决方案**：
- ✅ 活跃维护，定期更新
- ✅ JSONL 列化 + 字段过滤（原生桌面查看器里少见）
- ✅ 现代 UI（丹青框架）

> ⚠️ **ANSI 颜色不是我们的能力**（2026-09-13 核实并改正）。
> 本表原先把这一项标成我们的 ✅ / klogg 的 ❌ —— **两边都错**：`src/` 与框架里
> 都没有任何 ANSI 转义序列解析（全仓 `ansi` / `0x1b` 零命中，命中的 `escape`
> 全是键盘 `Escape` 键与正则/JSON 字符串转义）。klogg 缺 ANSI 是它的老 issue，
> 我们**同样缺**，这一项不构成差异化。
> 容易混淆的是：我们**按级别着色**（ERROR/WARN/INFO/DEBUG）—— 那是按内容分类上色，
> 与「把日志里程序自己吐的 ANSI 转义渲染成彩色」是两件事。**别把后者写进对外文案。**

### 2. 对 LogViewPlus：架构优势

**LogViewPlus 痛点**：
- 全量解析 = 结构性慢（116KB 文件搜索 15 秒）
- 表格化展示 = 滚动卡顿
- 价格高（$95 企业版）

**我们的解决方案**：
- ✅ mmap 秒开（结构性快）
- ✅ 虚拟化滚动（流畅）
- ✅ 价格优势（免费层 $0 对标 klogg；付费层 $29 低于 LogViewPlus $45）

### 3. 差异化功能：JSONL 列化

**市场空白**：
- VS Code 扩展：独立窗口，占编辑器资源
- 在线工具：隐私风险，不适合生产日志
- 无**原生桌面**的 JSONL 列化查看器

> ⚠️ **别写成「独家」**（2026-09-13 收口径）：我们自己的
> `DEEP_COMPETITIVE_RESEARCH.md` §5.1 就列着 LogViewPlus 支持 JSONL（基础）、
> VS Code 扩展 daucloud 支持 JSONL（GB 级虚拟表格 + JSON Pointer 过滤）。
> 站得住的说法是「**原生桌面、秒开的** JSONL 列化」—— 限定词别丢。

**我们的解决方案**：
- ✅ JSONL 自动检测
- ✅ 列发现 + 列化展示
- ✅ 字段过滤（level=ERROR status=500）
- ✅ 嵌套展开（user.id, tags[0]）

## 宣传材料

### 一句话定位

> **丹青日志 LogLens**：klogg 的速度 × LogViewPlus 的结构化
> 
> 原生桌面日志查看器，JSONL 秒开列化成表

### Show HN 稿件

**标题**：
> Show HN: 丹青日志 LogLens – klogg 的速度 × LogViewPlus 的结构化

**正文**：
> 你好 HN！
>
> 我开发了丹青日志 LogLens，一个专业的大文件日志查看器。
>
> **核心优势**：
> - 速度：与 klogg 同档（mmap 架构），1GB 文件搜索 69ms
> - 功能：JSONL 列化 + 字段过滤（原生桌面里少见）
> - 维护：活跃开发，定期更新
>
> **为什么做这个**：
> - klogg 停更4年，273 open issue 未解决
> - LogViewPlus 性能差，价格高
> - JSONL 日志无专业工具
>
> 欢迎试用！GitHub: https://github.com/14uncle/danqing-log

## 下一步

1. **提交优化**：性能已达标，可以提交
2. **准备截图**：使用 RustRover 运行配置生成截图
3. **发布宣传**：使用 Show HN 稿件发布
