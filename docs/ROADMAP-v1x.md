# ROADMAP: v1.x 付费层

- @author 十四叔
- @date 2026/09/12
- 状态: **未开工** —— v1 发布、前提③（首单外检）之后启动。
- **2026-09-12 用户裁决两项，本文件相应收口**：① **等级直方图改判进 v1.0**（自 §一 欠账表移出）；
  ② **MS Store 纳入 v1.0**，形态为**纯免费层上架**（trial / 买断基建仍留 v1.x）。
- 本文件是**付费层的唯一权威清单**。此前各项没有汇总，只以 `**Out**:` 的形式散落在 5 份 SPEC 里
  （`SPEC-core-viewer` / `SPEC-jsonl-table` / `SPEC-live-tail` / `SPEC-settings` / intent:52），
  且与「v1 全功能免费」的措辞互相打架 —— 本文件一并收拢并销案。

> 定位裁决与理由：`../../danqing/docs/intent/log-viewer-poc.md` §定价锚与渠道（2026-09-12 重裁）。

## 分层原则（一句话）

**免费层 = 看懂；付费层 = 批量 / 留存 / 交付。**

- 「看懂」= 读一批日志、搜、定位 → 免费。**缺一条就输给 klogg / LogViewPlus，收费即自杀。**
- 「批量 / 留存 / 交付」= 跨文件、跨会话、把结果交给别人 → 付费。
- **反例警戒**：把 v1 的欠账当付费钩子，用户会读成「扣走我本来就该有的东西」——
  这是付费墙最招骂的形态。凡属 v1 欠账者，一律进免费层。

---

## 一、免费层

### 已交付（截至 2026-09-12，人工验收已全过）

mmap 秒开 GB 级 / 行锚定虚拟滚动 / 正则搜索+高亮+命中导航 / live tail+实时过滤（增长检测、轮转截断重建）/
JSONL 自动检测+列发现+字段过滤迷你语法+嵌套展开 / 编码检测（UTF-8·UTF-16·GBK·Latin-1）/
级别着色 / **级别计数侧栏（2026-09-12 交付）** / 书签（会话内）/ 文本选区复制 /
深浅主题（持久化）/ 系统托盘 / 设置卡 / 异步打开 / 巨量追平

实测数字（release，本机 1GB，`logbench`；权威表见 `PERFORMANCE_REPORT.md`）：
明文 索引 92 ms · 搜索 115 ms；JSONL 索引 94 ms · 字段过滤 40 ms · 搜索 129 ms；
随机访问 0.52/0.58 µs/行；冷开 ~600 ms / 热开 ~90 ms（**2026-09-15 复测**；搜索为默认
大小写不敏感值，敏感时代旧值 71/69 ms 勿再用）；5GB 冷开 ~3 s、10GB 冷开 6~7 s
（2026-09-20 实测）—— 这是发布素材的弹药，也是免费层的价值证明。

### 欠账与缺口（补在免费层，不收费）

| 项 | 性质 | 出处 |
|---|---|---|
| 列宽拖拽 / 列显隐 / 列重排 | v1 欠账 | `SPEC-jsonl-table.md:30` |
| 书签持久化 | v1 欠账（**2026-09-12 由 v1.x 改判至此**） | `SPEC-core-viewer.md:29` |
| 时间戳列类型系统 | 打磨 | `SPEC-jsonl-table.md:31` |
| 免语法字段查询 UI（列发现采样结果做成下拉点选） | 缺口，**提议待裁** | — |
| 原始文本模式按级别筛选（.log 侧栏柱条可点） | 缺口，**2026-09-14 用户裁定挂 v1.x** —— 需新造「行首子串」过滤通路（计数口径 = 行首 200B，普通过滤 = 全行子串，直接接会「柱条数字与筛选结果当场打架」，违反 D2 口径一致原则） | `specs/SPEC-level-histogram.md` §5/D3 |
| 行多选（Shift 连续 / Ctrl 指定） | 缺口，**2026-09-14 用户裁定挂 v1.x** —— **前置 = 框架先给 `Event::MouseInput` 加修饰键**（该事件现在只有 `button/pressed/position`，`MouseWheel` 的 shift 是上次打磨寄生加的），属第三次联动；产品侧还要一整套新状态（多选集合/锚点、复制按显示行序拼接、子行怎么算、与三级复制链的优先级、`COPY_MAX_LINES` 超限检查、过滤/折叠失效守卫） | `specs/SPEC-selection-copy.md` §8 |
| 输入框 hover 反馈（P4） | 缺口，**2026-09-15 用户裁定本批不补 → 挂 v1.x** —— 框架 `TextInput` **连 hover 状态都没有**（`danqing/src/widget/form/text_input.rs` 里 `CursorMoved` 零命中），属框架改动 = 一次跨仓联动；产品侧无从下手 | `tasks/matrix-interaction-polish.md` §6.3 |

**等级直方图为什么必须免费**：它是 LogViewPlus 最能卖钱的一个功能（截图：Info 4,544,133 /
Error 43,464 / **Fatal 4,760**，一眼知道该看哪行），也是排障的第一动作。免费层缺它，
「不比 LogViewPlus 差」这句话当场就说不出口。实现便宜 —— 索引时顺带统计即可。

> **2026-09-12 改判 + 当日交付**：等级直方图原列本表（v1.x 免费层欠账），经用户裁决
> **做进 v1.0**（理由同上段：它是免费层对比话术的成立前提，不该让 v1.0 首发热缺这一项），
> 同日走完 spec → plan → build（T1–T8），机器部分闭环，**待人工验收与 review**。
> 实现见 `../SPEC.md` 地图的 `level-histogram` 模块 + `docs/specs/SPEC-level-histogram.md`
> + `tasks/plan-level-histogram.md`。
> 本表其余各项仍按「v1 发布后补」执行。

**免语法字段查询 UI**：Daucloud 的 `field pointer + condition + value` 三段式，外加「12 sampled fields」
把字段名摆出来给用户点。你的 `level=ERROR status=50*` 要求用户**先知道字段名、先学会语法** ——
这是**可发现性缺口，不是功能缺口**，而列发现本来就做了 512 行采样，把结果做成下拉即可。

**书签持久化的改判说明**：原记 v1.x。改判理由 —— 它是 v1 欠账不是新价值，锁进付费层会招骂。
但**工作台会话**（下节）是另一回事。

---

## 二、付费层（v1.x）

### 腿一：多文件时间戳合并 ★★★

- **做什么**：多个日志文件按时间戳归并成一条时间线；跨文件 `req_id` 跳转
  （一个请求在 auth → worker → db 之间追下去）。
- **为什么值钱**：四缺里唯一 LogViewPlus 有而你无的功能，也是「LogViewPlus 替代品」定位的入场券。
  LogViewPlus 靠 SQL 做跨文件查询，笨重且慢 —— 真空。
- **成本**：**高**。引擎 + UI 双改，正是 intent「三大技术风险」之一的多文件合并。
- **出处**：`SPEC-live-tail.md:27`

### 腿二：字段分析 ★★★

- **做什么**：对已提取字段做聚合 —— 数值列分布/分位数（`duration_ms` 的 p50/p99/最大）、
  枚举列取值分布（`status` 的分布）。
- **为什么值钱**：**手上唯一没被变现的资产。** 483 万行 235 ms 的字段提取能力（`memmem`，零 parse）
  已经证明，但目前只用来「找那一行」。运维问「昨天 p99 多少」，现在得写脚本或导进别处才能答。
  竞品要么慢（LogViewPlus 结构化 1GB 冷启动本机实测 60 秒）、要么没结构（klogg）。
- **成本**：**中低**。提取路径现成；分布是线性扫描，分位数需排序或近似算法（t-digest 之类）——
  仍**比腿一便宜一个量级**。
- **风险**：字符串值切取换真 parser 的边界问题（`SPEC-jsonl-table.md:16` 已记为待办），
  聚合是对提取结果的二次消费，边界错误会被放大 —— 腿二开工前需先解决这个。

### 腿三 / 腿四：交付与留存 ★★

| 项 | 内容 | 出处 |
|---|---|---|
| 导出 | 过滤结果导出、JSON 美化导出、CSV | `SPEC-jsonl-table.md:31` |
| 工作台会话持久化 | 命名会话（过滤器组合 + 搜索历史 + 列配置 + 展开态）跨会话保存/切换 | `SPEC-core-viewer.md:29`、intent:52 |

单独看都不构成购买理由，但它们是腿一/腿二的收尾：用户分析完，总得能把结果交出去、
把自己搭起来的工作台留下来。

> **与会话持久化的切分**：书签持久化 → 免费层（欠账）；**命名工作台会话** → 付费层（真增值，
> 对标 LogViewPlus Templates）。两者名字相近，别混。

### 基础设施（非卖点，但前置）

- MS Store 授权 / trial 轨、Store 更新轨：`SPEC-settings.md:68`
- 试用过期 → **降级到免费层，不是变砖**（这是整套试用方案能成立的前提）

---

## 三、渠道与收费

| 渠道 | 免费层 | 付费层 |
|---|---|---|
| **GitHub（主）** | 永久免费开源 | **$29** 买断（2026-09-12 定档） |
| **MS Store（辅）** | **v1.0 起上架**（纯免费层，无 trial、无内购） | v1.x 接 Store 内置 trial → 买断；**过期降级到免费层** |

- **不设订阅**（Dadroit 按大小收订阅正是其口碑裂缝，反向操作即差异化；产品线一致）。
- 企业档 **$59/seat**（2026-09-12 定档）—— 按 LogViewPlus 个人↔企业的 2.1 倍差折算；
  原 $89 在个人档降到 $29 后成 3 倍差，过陡。
- **为什么不是「v1.0 全功能 + 试用期」单轨**：见 intent §定价锚与渠道三条裁因
  （发布推迟 / 丢 GitHub 开源叙事 / 试用期撞生产事故）。
- **MS Store v1.0 为何不带 trial**（2026-09-12 裁决）：v1.0 交付物 = 免费层单二进制，
  Store 此时只当**第二个免费分发渠道**；接 trial 等于把付费层基建（授权轨 / 过期降级）
  前移进 v1.0，正是上面「试用期撞生产事故」被否决的那件事。Store 只做打包 + 上架。
- **前提③（首单外检）的判定点随之后移**：v1.0 两个渠道都是免费层，**不存在购买路径**——
  首单只能在 v1.x 付费层上线后产生。原「MS Store 试转付费的首单即可判定」不再适用于 v1.0。

---

## 四、待裁项

- [x] ~~个人档价格~~ **已定 $29（2026-09-12）**。理由：免费层已给出 LogViewPlus 七成功能，
      $29 让价格与事实一致；$39 会重新引入「同价比较」压力，而功能少一档的事实答不上来。
      低于 LogFusion $35 上沿，仍高于「一杯咖啡」的心理线。
- [x] ~~企业档~~ **已定 $59/seat（2026-09-12）**，对齐 LogViewPlus 个人↔企业的 2.1 倍差。
- [x] ~~等级直方图归 v1.0 还是 v1.x~~ **已定做进 v1.0（2026-09-12）**，理由见 §一 改判块。
- [x] ~~MS Store 是否纳入 v1.0、以什么形态上架~~ **已定纳入 v1.0，纯免费层上架（2026-09-12）**，
      trial / 买断留 v1.x，理由见 §三。
- [x] ~~行多选（Shift 连续 / Ctrl 指定）是否进 v1.0~~ **已定挂 v1.x（2026-09-14）**。
      裁定理由：① 连续区间的复制**框选已经能做**，Shift 行选是便利增强不是能力缺口；
      ② Ctrl 指定多选在日志排查里偏 niche；③ **成本侧的真前置在框架** —— `Event::MouseInput`
      不带修饰键，要先改框架（第三次联动），再加产品侧一整套状态与失效守卫，是一个 M5 级模块；
      ④ v1.0 已经为这批阻塞了一轮，再加模块 = 发布再延、验收再跑一遍。**且「批量」正是
      付费层（批量·留存·交付）话术的天然素材，放 v1.x 叙事更顺**。落表见 §一 欠账与缺口。
- [ ] 腿三/腿四首发是否全上，还是只上导出
- [ ] 免语法字段查询 UI 归免费层还是作为打磨项（倾向免费层，见 §一）

## 五、前置依赖

- ~~v1 发布 + 人工验收通过（在途遗留项：async-open 10GB 冷开复核 / 索引中 Ctrl+O 取消 /
  轮转重建旧内容可见；text-selection 实机五种姿势）~~ —— **已满足**（2026-09-12 人工验收全过，
  async-open 五项 + text-selection 五种姿势；功能闭环亦已闭）
- ~~当前工作区尚有未 commit 改动（`src/settings.rs` / `src/view.rs`），跨仓 danqing 亦有未提交项~~
  —— **已清**（两仓 working tree 均 clean，`dev` 无领先 origin 的提交）
- **v1.0 收尾工作面**（不属本文件付费层，但挡在 v1.x 前面）：
  ~~等级直方图~~（**已交付**，待验收）· ~~README 重写~~（**已完成**）/
  **余下**：MSIX 打包 + Store 上架 / 版本号 `1.0.0` + git tag
- 腿一依赖 live-tail 已有的增量索引能力（`append_from` / `lines_from` / `run_filter_from`）

## 六、相关文档

- 定位与定价裁决：`../../danqing/docs/intent/log-viewer-poc.md`
- 竞品：`COMPETITIVE_ANALYSIS.md` / `DEEP_COMPETITIVE_RESEARCH.md` / `PRE_LAUNCH_RESEARCH.md`
- 原始 SPEC（各项出处）：`specs/`
- **2026-09-12 定价口径收口**（审计清单，防复发）——以下七处的过时报价 / 付费墙措辞已全部
  对齐 `$0 / $29 / $59`：`PRE_LAUNCH_RESEARCH.md`、`COMPETITIVE_ANALYSIS.md`、
  `DEEP_COMPETITIVE_RESEARCH.md`、`COMPETITIVE_GUIDE.md`、`PERFORMANCE_REPORT.md`、
  `SPEC-settings.md`（Never 措辞修订）、`handoff-app-chrome-2026-09-06.md`（张力销案）。
- **营销口径已收口（2026-09-12）**：`DEEP_COMPETITIVE_RESEARCH.md` 的 `klogg 800ms` 是一个
  **从未实测过的数字**（intent:76 记 klogg 打开 1GB 为「秒级，用户体感，**无精确读数**」），
  写作精确值属编造 —— 而它撑着该文档 **6 处对 klogg 的「碾压」主张**，与 intent:80 的明确修正冲突
  （**碾压对 klogg 不成立**：mmap 同构，速度平手；碾压仅对 LogViewPlus 成立）。已全部清理：
  §2.2 性能基准表 klogg 列改「未实测」并加口径修正框、删「快 1.4 倍」一项、§6.1 删整行、
  §6.5 删 klogg 行、§5.1 / §5.2 / §7.2 三处主张加限定（用户裁决 2026-09-12）。
  **口径基准**：对外讲「碾压 LogViewPlus（全量解析入表格 = 结构性的慢）」+「快 Daucloud 12 倍」；
  对 klogg 只讲停更 4 年 / 273 open issue / 无 JSONL / 无 ANSI 颜色 —— **不讲速度**。
  注：`COMPETITIVE_GUIDE.md` / `COMPETITIVE_ANALYSIS.md` / `PERFORMANCE_REPORT.md` 三处
  原本就写的是「与 klogg 同档」，口径正确，未动。
- **2026-09-21 二轮口径清扫（用户发起，投稿前清障）**——首轮收口之后新查出的问题已全部修掉：
  ① `COMPETITIVE_GUIDE.md` / `COMPETITIVE_ANALYSIS.md` 的 **ANSI 颜色 ✅（假主张）** 改 ❌
  （与首轮 PERFORMANCE_REPORT 的甲案同类，当时漏了这两份）；② 两份文档多处「JSONL 独家」改
  「原生桌面、秒开」限定式；③ 全仓敏感时代旧值（搜索 69ms / 过滤 70·74ms / 索引 77·88ms）刷新到
  2026-09-15 复测值（115/129ms、40ms、92/94ms），历史表保留但注明日期与敏感时代；④ 竞品
  「秒级/快/慢」模糊格全改「未实测」（klogg 从未实测；LogViewPlus 仅 1GB 冷启动有实测 60s）；
  ⑤ `DEEP_COMPETITIVE_RESEARCH.md` §2.2 的 10GB 竞品行（~600s/~70s 标为实测）实为外推，
  改「未实测」——竞品侧只实测过 1GB（用户 2026-09-20 澄清）；⑥ 回填新实测：VS Code 明文
  1GB ≈10s、自家 5GB 冷 ~3s / 10GB 冷 6~7s（2026-09-20 用户实测）；**旧目标「10GB <3s」
  未达成，禁止当成绩引用**；⑦ 「卡死/卡成 PPT」类夸张措辞（README、launch-copy 稿 A/B/C/E/F、
  本文件 §二、SPEC-v1x-field-analytics）改实测值表述；⑧ 「116KB 文件搜索 15 秒」单条论坛
  引语不再作性能证据（来源链接缺失），对外一律用本机实测 60s；⑨ 三份 POC 期中文 Show HN 旧稿
   tombstone，定稿见农场 `danqing/docs/intent/curated-channel-submission-plan-2026-09-20.md` 文案 3。
  **遗留项已定案**：「klogg 把 GBK 中文日志认成 ISO-8859-1」系编造句（用户 2026-09-21 确认
  未实测），**且当日被实测证伪**——klogg 打开 `demo-cn-gbk.log`（GBK，32 MiB）中文显示正常。
  README / launch-copy×3 / ms-store-copy §截图 共 5 处已撤，并入 `ms-store-copy.md`
  禁止声称清单（同批入清单：竞品 10GB 外推值、「卡死/卡成 PPT/拒开」、「116KB/15s」引语当证据）。
  推论：DEEP §5.3「竞品编码普遍差」的笼统叙事对 klogg 不成立；该节已补 2026-09-21 实测表
  （同一 GBK 文件默认打开：LogViewPlus 乱码 / klogg 正常 / 我们正常且中文可搜）。
